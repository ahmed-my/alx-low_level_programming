#include <stdio.h>
/**
 * main - entry point
 *
 * program to print all single digit number
 * Return: Always 1 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
