#include "main.h"
/**
 * _abs - Absolute value of a number
 * @num: The number to be checked
 *
 * Return: Success 0
 *
*/

int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
